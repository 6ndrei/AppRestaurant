<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color of the CardView -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white"/> <!-- Change to your desired background color -->
            <corners android:radius="10dp"/> <!-- Match this to your cardCornerRadius -->
        </shape>
    </item>
    <!-- Stroke definition -->
    <item android:left="5dp" android:right="5dp" android:top="5dp" android:bottom="5dp">
        <shape android:shape="rectangle">
            <stroke android:width="5dp" android:color="@android:color/black"/> <!-- Change to your desired stroke color -->
            <corners android:radius="10dp"/> <!-- Match this to your cardCornerRadius -->
        </shape>
    </item>
</layer-list>
